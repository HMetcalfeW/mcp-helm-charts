apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlassian.fullname" . }}
  labels:
    {{- include "atlassian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atlassian.selectorLabels" . | nindent 4 }}
